import { load } from "../load";

export const Profile = load("profile");

export const VenueManager = Profile.venueManager;

export const ProfileName = Profile.name;

export const ProfileAvatar = Profile.avatar;

export const ProfileEmail = Profile.email;
